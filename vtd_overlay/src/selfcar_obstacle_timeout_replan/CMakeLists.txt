cmake_minimum_required(VERSION 3.14)
project(selfcar_obstacle_timeout_replan)
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_python REQUIRED)
ament_python_install_package(${PROJECT_NAME})
install(PROGRAMS scripts/obstacle_timeout_replan scripts/aeb_object_filter DESTINATION lib/${PROJECT_NAME})
install(DIRECTORY config launch DESTINATION share/${PROJECT_NAME})
if(BUILD_TESTING)
  find_package(ament_cmake_pytest REQUIRED)
  ament_add_pytest_test(test_policy test/test_policy.py)
  ament_add_pytest_test(test_cloud_geometry test/test_cloud_geometry.py)
endif()
ament_package()
