#!/usr/bin/env bash
set -euo pipefail
script_path="$(readlink -f -- "${BASH_SOURCE[0]}")"
repo_root="$(cd -- "$(dirname -- "$script_path")/../.." && pwd -P)"
exec "${CARLA_PYTHON:-/home/a/CARLA/venv-0.9.16/bin/python}" "${repo_root}/tools/carla_scenario_gui.py" "$@"
